Created updateState()
    - Need to implement zobrist hashing so I can check it in "Update state"
        - randomly generated piece-square zobrist hash table
        - randomly generated board state variables for:
            - 4 for castling
            - 2 for en EN_PASSENT
            - 2 for turntomove
        - updateZobrist()
        - Method to check for 3 and 5

Created generateZobristTable()
How to update zobrist?
- Square by square: Avoids move logic considerations (en passent, castling) by looking at current vs previous board state
- Move-informed: Use last Move information to only 'touch' the squares that have changed. More work upfront, faster execution?

Move-informed:
- MOVE:
    - ORIGIN: newPiece(origin)
    - DESTINATION: newPiece(dest)
- ATTACK:
    - ORIGIN: newPiece(origin)
    - DESTINATION oldPiece(dest), then newPiece(dest)
- EN_PASSENT:
    - ORIGIN newPiece(origin)
    - DESTINATION newPiece(destination)
    - EP_SQUARE oldPiece([destRank][destFile + 1 (-1 depending on turn)])
- CASTLE_LONG:
    - ORIGIN newPiece(origin)
    - DESTINATION newPiece(destination)
    - LONG ROOK SQUARE Rook(destination - 2)
    - NEW ROOK SQURE Rook(destination + 1);
- CASTLE_SHORT:
    - ORIGIN newPiece(origin)
    - DESTINATION newPiece(destination)
    - SHORT ROOK SQUARE Rook(destination + 1)
    - NEW ROOK SQURE Rook(destination - 1)
- PROMOTE_ATTACK:
    - ORIGIN newPiece(origin)
    - DESTINATION oldPiece(destination), promotedPiece(destination)
- PROMOTE_MOVE:
    - ORIGIN newPiece(origin)
    - DESTINATION promotedPiece(destination)

Finished board state zobrist updating
What about castling? Update zobrist through 'setters' for castling rights

Modified En Passent zobrist encoding to record different hashes for each possible EP square
- Added another final hashMap epHashTable to store hashes corresponding to all possible EP squares (only 16, 2 ranks)

Creating game-end method to detect and execute different game ending conditions.
- Submethod for Insufficient material
- Submethod for 50/75 halfclock draw
- Submethod for 3/5 fold repetition
- Not currently concerned about time controls
- Checkmate/Stalemate? Valid moves are generated before calling this function, then provided to this function so it can check length for checkmate/stalemate

Fifty/Seventy five halfclock draw:
- Implement halfclock
    - primitive int class attribute halfClock = 0;
    - Added updateHalfClock(), implemented in updateState()
    - Added checkNMoveDraw(int n) to check for 50/75 move draw, implemented in updateState and evaluateGameEndConditions
    - Added checkNFoldRepeat(int n) to check for 3/5 fold repeat draw, implemented in updateState and evaluateGameEndConditions
    

---------------------------------------

Added toString to board to print the board representation

Forgot to update the bitboard occupancy for each played move - 

remove turn parameter from updateState(int lastPlayerSign), read it from the playedMoves deque

Separate and parse moves
([KQRBNkqrbn])?([a-hA-H])?([1-8])?(x)?([a-hA-H])([1-8])(=[QRBNqrbn])?([+#])?|(O-O-O)|(O-O)

Separate games in pgn
(?s)(.*?[ ]+[01][.\/]?\d?-[01][.\/]?\d?)

Get moveset from 'game'
(?s)\n1\..*?[ ]+[01][.\/]?\d?-[01][.\/]?\d? // With newlines
(?s)] ?1\..*?[ ]+[01][.\/]?\d?-[01][.\/]?\d? // Without newlines

Separate metadata from game
((?:\n?\[.*\]\n)+)

Separate each of 7 mandatory meta fields:
\n?\[Event "(.*?)"\]\n|\n?\[Site "(.*?)"\]\n|\n?\[Date "(.*?)"\]\n|\n?\[Round "(.*?)"\]\n|\n?\[White "(.*?)"\]\n|\n?\[Black "(.*?)"\]\n|\n?\[Result "(.*?)"\]\n

I can't really create a "Move" without seeing the board, Specifically due to En passent pawn moves as either its an invalid capture or 
a valid en passent depending on the board occupancy state.

Move validator from PGN info:
1. Determine moving piece
2. Determine destination square
3. Origin square:
    - No disambiguation rank or file:
        - generateValidMoves() for the current board state
        - Ensure only ONE move matches the piece and destination
        - If multiple moves satisfy, then invalid move/pgn
    - Disambiguation provided
        - generateValidMoves() for current board state
        - Use disambiguation to filter moves
        - If both disambiguations are provided just search for the specific origin square
4. Move type
    - Move/Attack are distinguised by the presence of group 4 (group4 != null)
    - Promotion move/attack are distinguised by the presence of group 7 + group 4
    - En passent must be validated by looking at the presence of a pawn one rank 'behind' the moved pawn, that just moved two spaces

Order:
1. DETERMINE CASTLING, search for validity, add move
2. Otherwise determine piece, destination, search for validity with or without disambig.
3. Once move is validated, determine if en passent or other type, add move.
4. "Play move" on board and update state for next move validity checking.

// File disambiguation
[2, 1, 12]  // Knight from b1 → e2  
[2, 6, 12]  // Knight from g1 → e2  

// Rank disambig
[4, 0, 16]  // Rook from a1 → a3  
[4, 24, 16] // Rook from a4 → a3  

// Rank and file disambig
[5, 3, 12]  // Queen from d1 → e2  
[5, 39, 12] // Queen from h5 → e2  

Added helper functions to find moves in an ArrayList<int[piece, origin, destination]> using the information from a PGN move
- public static boolean moveInMoveset(int[] move, ArrayList<int[]> moveset);
- public static int[] getMoveInMoveset(int piece, int dest, ArrayList<int[]> moveset);
- public static int[] getMoveInMoveset(int piece, int dest, ArrayList<int[]> moveset, int dsRank, int dsFile) 
    -(Method pararmeter overload for optional origin square disambiguation)

Tested these two functions and overloaded functions in test.findInMovesetTest()

Preliminary moveValidator test passed, test with larger games with more varied pgn notation
Testing well with larger games, going to use a brute-force method to find all possible bugs in the code,
just feed as many games through as possible and detect errors

Known bugs: 
- Using mask > 0 to detect the presence of any flagged bits
    - Bad as the numeric representation could be negative!
    - Change to mask != 0 instead

- Fixed generateKnightMoveMask() for origin 14 specifically, wasn't giving possible 14-->31 movement (ForwardRight)
- Need to incorporate promotion piece, currently I invoke a method to "ask" for what promotion piece, but if reading from PGN I need to extract the promotion piece


Created separate validateMove() to validate a single move in algebraic notation

Terminal loop notes:
- Differentiate between move vs capture, currently only checks friendly occupancyR

Added regexes as static class variables for RegexParser

Modified validateMove() to properly reject capture moves that aren't formatted as capture moves (no x, only move)

Added non-static method overload boardVisualize() which visualizes the current pieceboard for the Board object, 
rather than being supplied the board in the parameters.

Updated validateMove() to reject invalid promotion pieces for promotion moves

TODO:
- Invalidate pawn moves to the 8th or 1st rank without a promotion! [Done] - Must be done in move validation (validateMove()), generateValidMoves() only concerns the movement itself
- Fix weird pawn capture invalidation for 4th and 5th rank as black and white respectively DONE - issue with regex captitalization of files vs pieces


Added additional exit codes for evaluateGameEndConditions() to signify which conditions are met
-----------------------------------
Create javadocs for required .java files for LanternaChess
- engine\Board
- engine\Move
- parser\RegexParser
- lanterna\LanternaChess
- exceptions\AlgebraicParseException

Removed runLanterna.bat as that was for Testing

Found bug in En passent zobrist hashing
- Was generating for all destination squares instead of just file
- Need to change updateZobrist to reflect this
Stopped javadocs at generateEPhashtable to fix this bug [Done, changed like 4 things]
---------------------------------------------------------------------------------------------
Back-seeking/undoing move
- Castling rights
    - Unknown when castling rights were set to false
    - Can't determine whether current unplay move was the first to unset rights
- Implement board history?
    - Boards
    - Bitboards
    - Castling rights
    - Zobrist hash
- Update incrementally
    - Halfclock
    - Claimable draws (50 and 3-fold)
    - Check

To keep it all neat and tidy and logical, will implement a history of all variables, read the last one for backtracking.

Added
- boardStateHistory
- bitStateHistory
- castlingHistory
- epHashHistory
- boardStateHistory

Changed playMove to add the last board to boardStateHistory before playing the move.
playMove adds bitstate to history
Changed bitstate from Long to long

Changed Board.boardState to board
Modified updateZobrist to work with epHashHistory instead of LastEPHAsh
- Also to work with boardHistory instead of lastBoardState

Updated generateCurrentZobristHash to use epHashHistory instead of lastEpHash

"Reverse play"
- Using history dequeues
    - zobrist through zobristHistory (and repetition draws)
    - board through boardHistory
    - state through stateHistory
    - bitState through bitStateHistory
    - castling rights through casltingHistory
    - halfclock history
- Re-generated after unplay
    - fiftyMove/ThreeFoldDraw available
    - whitesTurn

---- Committed to dev2 and merged dev2 with main
---- Branched dbDev:
- Re-format GameData to include only metadata fields
    - Relegate any additonal fields to a dynamic sized array 
    - Store these addtional fields as a blob? in SQL database.
- Created new RegexDatabase.java 
- Created new RegexGameData.java
Currently working on RegexDatabase.writeDB() - About to implement batch writing after creating RegexDatabase.setValues()

Duplicate checking does not work!!
FIRST change ID hashing to something better (murmurhash)
DONE - Changed to byte[16] representing a 128bit hash - MUCH faster writes to SQLDB

Changed RegexGameData ID to byte[16] = 128 bit murmurhash3 hash
Changing RegexDatabase to accomodate changes ^^
- SQL DDL for Table
- .setInt() to .setBytes() in setValues()
DONE

Now fixing removeDuplicates()
==============================
Starting to modify LanternaChess to include game replay functionality

  _____ _    _ ______  _____ _____ 
 / ____| |  | |  ____|/ ____/ ____|
| |    | |__| | |__  | (___| (___  
| |    |  __  |  __|  \___ \\___ \ 
| |____| |  | | |____ ____) |___) |
 \_____|_|  |_|______|_____/_____/ 

 Widen console to both accomodate splash screen and move list on the right side? or other information

 Terminal size from 28, 20 to 38,20 to 42, 20 to 45, 20 to 48, 20 to 51, 20-

 Working on displaying PGN moves as game is played, which requires playedMoves --> PGN functionality
 - Algebraic notation requires CHECKS
 - Began working on "Get moves as string array" but now thinking I should have a board variable
 PGN moves that creates and records moves in PGN format as game progresses.

 Created recordAlgebraicMove()
 - Takes the last played move in the current object's playedMoves and converts it into algebraic notation
 - Then adds to algebraicHistory

Updated updateBoard() to include recordAlgebraicMove()

updated undoLastMove() to remove the last added string to algebraicHistory

Now incorporating it into LanternaChess
wasted a bunch of time creating recordAlgebraicMove() the WRONG way (Specifically Disambiguation)
Disambiguation, Check, Checkmate are the only board context dependent variables not included in MOVE

MUST FIX it as currently it won't generate valid algebraic notation (in terms of disambiguations)
FIXED - 
- Modified game loop logic, now evaluateGameEndConditions() is called in updateState() due to how 
algebraic move generation requires information from both the previous board state
- Initialize Board -> playMove -> updateState() -> playMove()
- Now takes in the previous valid moves, and returns the new valid moves
- Backwards compatible: Running evaluateGameEndConditions multiple times has no effect as it just
sets the board state depending on certain conditions, conditions which wouldn't have changed between calling
updateState() and evaluateGameEndConditions() right afterwards, so this change does not break existing loops, 
only perhaps makes them slightly less efficient
- Modified lanternachess to accomodate these changes




*** Adjust chess board rendering using BACKGROUNDS and MONOSPACED FONTS, 
fonts set through accessing Jpanel from terminal.getComponent()?

test disambiguation algebraic generation!@!
Combine updateState and evaluate game end conditions in the loop

Changed


update javadocs

Working on drawing the replay menu, then have to figure out replay loop


Blob blob = resultSet.getBlob("moves");
                byte[] byteMoves = blob.getBytes(1, (int)blob.length());
                ArrayList<String> extractedMoves = new ArrayList<>(); 
                
                try(ObjectInputStream ois = new ObjectInputStream(new ByteArrayInputStream(byteMoves))){
                    extractedMoves = (ArrayList<String>) ois.readObject();
                }
                catch(Exception e){
                    System.out.println("filterReadOnPlayer(): Exception occurred deserializing move data");
                    return retArray;
                }

Now nothing displays wtf
also fix scrolling function for a non-full page in playerCountLoop

New commit:
Implement Memento pattern for board history
Affected attributes:
- bitStateHistory/ bitState
- boardHistory
- stateHistory
- algebraicHistory
- halfClockHistory
- 

Histories tied to game logic:
- epHashHistory
- playedMoves
- zobristHistory
- 

playedMoves is tied to board, less so history

CHANGING TO SQLITE:
Begin with changing writeDB and DDL, Queries
- Change ID to 64 bit INTEGER, for now just truncate or h1 ^ h2, can change hashing algo later
- Change DATE to long (time since epoch)
- Change all VARCHAR to TEXT
- round float to REAL
- Change MEDIUMBLOBs to BLOBs

Remember to change removeDuplicateGames()

Deleted mysql service related methods from RegexDatabase.java

Ran into issue where I cannot define the resources source path with VSCode's launcher as getResources() searches the classpath of the built project (in temp folder) rather than
the actual src/main/resources=


JAVAFX

MousePress:
- Clicking on a Square
    - Have Selections
        - Square == selectedSquare
            - Set selections (Same)
            - Set piece pos (same)
            - Set parenthood to anchor (same)
        - Square != selected Square
            - Attempt MOVE
    - Do not have selections
        - Square is empty
            - Nothing
        - Square is occupied
            - Set selections
            - Set piece pos
            - Set parenthood to Anchor
- Clicking on NOT square
    - Clear selections
    - clearSelectionsFlag = false;

Mouse Release:
- Have selections
    - Square == selectedSquare
        - Snap piece back to square
        - clearSelectionsFlag == true
            - Clear selections
            - Toggle flag
        - clearSelectionsFlag == false
            - Toggle flag
    - Square != selectedSquare
        - IF I still have selections at this point, it means I've dragged the piece here
        - Attempt move
        - Clear selections
- NO selections
    - Nothing happens

getBoardStatus():
- All 4 castling rights
- Halfclock count
- nFoldRepeat count
- Zobrist hash length
- Current BoardState
- Current turn

Create new restorePiece() method and replace instances of movePiece being used to restore, 
rather than making an actual move!

--
GUIEngine.attemptMove() return codes integration with game ending GUI elements

--
Currently figuring out GUI-TIMER-ENGINE communication using concurrently running timer,
- TIMER->GUI callbacks for clock tick and timeouts
- ENGINE->TIMER callbacks for increment/game end conditions

Direct method calls:
GUI->Engine game end due to timeout


Timer seems to work, more precision timing required

TODO - Implement callbacks between clock and gui
I think really the only callbacks needed are:
    1. clock->gui clock tick
    2. clock->gui Timeout

- toggle/increment can be called by GUIEngine directly and then directly on clock
- gameEndConditions from board-> GUI and board->clock direct calls

Sept 8th - 
Add clock field to GUI Engine
- Added clock to GUIEngine attemptMove(), checks if clock exists and if so acts accordingly
- Added non-FX thread methods to GameSceneController to change clock displays
- We DO NOT need events, just run changes to GUI elements from other threads through Platform.runLater()

- Need to link callback targets for ChessClock
    - Link tick calls to non-FX methods on Controller
    - Link timeout calls to both? Board and Controller
        - Board saves game as PGN, and other stuff
        - Controller freezes clocks and displays game over, and reason
            - probably better to propagate to Controller directly rather than -> board -> controller
            - Other game-end conditions are still initiated by the FX thread, but timeout would be from the clock thread
            - ChessClock is already set up to send callbacks

** Fix chess clock to use consistent time increment for timer (millis vs nanos);
- Increments and Tick rate is provided in seconds
    - both are converted to ms, increment is added to timer, tick is provided to system.sleep()

- Added callback methods to ChessClock
    - Reformatted callbacks to be stored as an array of Callback, fits the observer pattern and futureproofs in case I want to callback to additional objects
    - Fixed callbacks in the chess timers themselves
- Added callback setting method to GUIEngine
- Actually set callbacks in ScratchFX

Overloaded GUIEngine constructor with an additional constructor taking clock time, increment and ticks, and a target for clock tick callbacks
automatically adds GUIEngine's board as a timeout target for ChessClock

ChessClock callbacks:
- ChessClock is created with empty lists for callbacks
- If GUIEngine is created using constructor with clocks, GUIEngine will automatically
add its Board as a timeout target
- Additonal timeout and tick targets can be added through GUIEngine.addTimeoutCallTarget() and GUIEngine.addTickCallTarget()

Controller and GUIEngine setup:
1. Load FXML, get controller reference from FXML loader
2. Create new scene, set as root (Might change with the addition of scenes)
3. Create GUIEngine (either with clock or without)
    - if with clock, also set Controller as Timeout and Tick target
4. Assign guiEngine object as the Controller's GUIEngine


Second ticks won't work well, need higher granularity - change ticks back to milliseconds and do like 500 or smth

9/10
In order to kill ChessClock threads, we must attach a listener to the stage to listen for window close event, and 
in response execute the ChessClock stop methods to prevent undead timer threads

Killing non-FX background threads:
- Create closeable interface for all controllers for scenes, implement and create cleanup() methods for non-fx threads or other cleanup duties
- Load all controllers and scenes at startup, keep list of references for controllers (maybe keep active controller as well)
    - ArrayList<Closeable>
- Create onWindowClose handler for main Stage to listen for window closes and either:
    1. Call cleanup method on CURRENT controller or
    2. Iterate through ArrayList<Closeable> and call cleanup on ALL controllers

Added - 
- Closeable interface - implemented by GameSceneController
- GameSceneController implementation of cleanup()
- cleanup() method in GUIEngine NOT related to Closeable interface (since Scenemanager would interact with controller, which then interacts with GUIEngine)
    - This method simply invokes ChessClock.stop() - which kills timer threads
- Rudimentary SceneManager Class - currently basic but may be good for future functionality
    - Simply an ArrayList<Closeable>
    - has cleanup() method that iterates through ArrayList<Closeable> and invokes Closeable.cleanup() on those classes
- ScratchFX.onWindowClose() - Attach to stage through stage.addEventHandler(WindowEvent.WINDOW_CLOSE_REQUEST, this::onWindowClose)
    - Invokes SceneManager.cleanup() - Again useful since each controller can do its own custom Closeable.cleanup() implementation.

Works! :)

Fire tick call right at toggle? - Doesn't make sense when tick rate is small enough
- Timers don't pause until they exit the tick sleep period anyway, and immediately after

ChessClock.toggle() first pauses timer, then adds increment to the player's time - However this increment is reflected in the last tick 
sent by the timer before its pausing which is strage because the timer is paused first, then the increment is added.
- Likely because when ChessTimer.pause() is invoked, the thread is still sleeping for the time specified by the tick parameter, so 
    the main thread continues execution, and when the timer thread wakes it sees the incremented time, and sends that in the callback
- At what ms value for tick does this no longer guarantee that the increment is added first? and would I ever use that level of granularity for
    the chess timer?

Answer: 100ms is good, its only around ~10ms that race conditions may occur
- ChessClock is definitely overengineered but it works
- Next time just use timestamp differences! (System.nanotime()) - No thread spawning, just the thread running ChessClock

FXML initialize()
- I was overriding initialize() and adding some init tasks to it, but realized there is no point to doing so
- It is NOT SAFE to get dimensions in initialize() so might as well do every initialization task in a custom method
- Removed override of FXML initialize() in GameSceneController, moved code into init()

Added 'game over' screen in controller, invisible and mouse transparent until game ends

9/13
Get GUI Controller to recognize different game end codes, display different game-end screens
- Added overloaded GUIEngine constructor for when white and black have different times
- Added accessor methods for time left in ChessClock and GUIEngine
- GameSceneController.init() now checks if the GUIEngine has a clock through:
    - GUIEngine.isTimed() -> checks if GUIEngine.clock is null or not, returns boolean
    - if (timed) {set player clock initial times} else {set clocks as non-visible}

issue: After white's first move the clock does not reflect the first increment until it is white's turn again.
- Likely because white's first move "starts" the clock, so there is no sleeping thread to 'wake' and send the tick call
    before the other timer is toggled
- Instead of changing ChessClock and ChessTimer, will just read the time left on the player's clock through GUIEngine.getTimeMillis()
    after the player plays a move.
Done
- Added GameSceneController.updateClock(), which updates both player's clock
- Inserted updateClock() into onMouseRelease() in to the case where the move is valid and is played on the board

Back on task: 
1. Configure controller to respond to game-end conditions
2. Test all game end conditions
    - White/Black timeout - works
    - White/black checkmate - Works
    - stalemate
    - 75 move draw - works
    - 5 fold repetition draw - works
    - Insufficient material - works for KvK
3. Work on end game conditions not already accounted for:
    - Mutual draw
    - Resignation
    - Claiming 50 move / 3 fold repetition draw
4. Test everything to date from Black's perspective as well


issue: Multiple end game evaluations occur at game-end, starts as checkmate, evaluates again and ends up as stalemate
- Caused by Board.evaulateGameEndConditions() being executed twice, once in GUIEngine.attemptMove(), and once in Board.updateState()
    - evaluateGameEndConditions() sets Board.state depending on current state, moves, and turn
    - First execution: validMoves.size() == 0 + Board.state == CHECK --> Board.State = CheckMate
    - Second execution: validMoves.size() == 0 + Board.state != CHECK (== Checkmate) --> Board.State = Stalemate
    - Method sets current state based on previous state, so multiple executions will change state without any other changes

Fix:
Take the call to evaluateGameEndConditions() out of updateState, as its initial purpose there was to emulate gameplay, which is now
being handled by the JavaFX GUI
- Also added a condition in where stalemate is only evaluated for board states not equal to CHECK, or any MATE (Basically just IN_PLAY, we can only reach stalemate from normal play obiously not from checkmate or ended game state)

Issue: Thought turn wasn't being tracked, forgot there's a flag in Board that tracks it.
fix: Created a getter Board.getTurnBool() to return Board.whitesTurn, created GUIEngine.isWhitesTurn()

Facade pattern/Wrapper methods
- Seems redundant to write so many methods just to keep up encapsulation and pass data to the outer most object
- However this supports encapsulation and is similar to a Facade pattern, thats why it feels strange/wrong, its because I am in fact making
    a tradeoff between efficiency/effectiveness for readability/maintainability
        - I pay for readability with tedium/redundancy

Tested Checkmate, forced repetition and move count draw, King v King insuff. mat.
- Implement timeoutCallback() calling endGame() through Platform.runLater()

finshed restart and exit

Added quick restart button to in-game screen 

9/14

Task: Still want to keep testing, but need the ability to initialize from black perspective. This would require changing
how I create the GUIEngine, so may as well start working on a title screen and the transition from title to in-game through
a game-setting screen - which would also allow me to change the settings and test from different perspectives
- Create and implement menu screen
- Figure out scene transitions/Scene loading/unloading
    - Create game using settings from scene transition
- Figure out a way to test everything programmatically rather than manually

future idea: Create a "TestEngine" for Board to read public PGNs and play them out using Board, which would
test functionality for board
- Ensure same outcome 
- Ensure no errors for any moves (curate complete games from PGN)
- Many PGN games would end in resignation or mutual draw
    - Code for these inputs? or just use games as-is and ensure validity up until the ending move?
    - Mutual draws and resignations aren't really a part of the game logic, its more to do with User interaction
        with the UI and relaying data between clients and the server in multiplayer

issue:
Using properties + listeners vs propagating custom events in order to get a signal out from the controller?
- Properties is controller-specific, would have to re-attach all listeners at each scene recreation
    - Something like an int property, with 0 being no change, and other numbers corresponding to the scenes to change to
- Propagate event from UI, listener on Stage
    - Event could contain the target destination
    - Requires custom events

Decided just to have specific properties on each controller for possible navigation 
- Listeners listening to Controller properties are attached when the scene is loaded
- Stage listeners (listening for ON WINDOW CLOSE) are attached in start() on the initial run

Gym and Laundry break - Todo after:
- Game creation from settings
- Test game win conditions, functioning UI from Black's perspective

issue: slight issue with pop-in loading of nodes when loading game scene
- Likely due to node manupulation on a scene while that scene is the root of the (already showing) stage
- Must finish modifying nodes BEFORE invoking stage.show()
    - However I need the scene reference to be used IN init to set the right click filter
    - But I can't get the scene through root.getScene() since the scene hasn't been set
fix: create new temporary scene object so we can associate the root to the scene before setting the stage
works! no pop in left, although a bit of a delay between hitting restart/play game and the game rendering
- Perhaps turn off background rendering for the GUIPieces?

new issue: Slight lag when restarting - Likely due to reparsing FXML
- Not a big deal, it'd just be nice to have to more responsive
- Could reset controller state and scene instead of re-loading 
fix:
- Not crucial, fix later

Settings screen - 
- White time
- black time
- white increment
- black increment
- player's perspective

issue:
Passing information between controllers, when controllers only live as long as each scene
    Delves into larger issue: Dynamically kill and load scenes? or store them with the ability to "reset" them and switch between?
        - "Resetting" the game screen would require 2 base states, one for each player perspective
        - Other scenes can be much more easily reset
        - "Reset and Switch" method will also solve previous 'lag' issue incurred by the FXML loader
    Possible solutions
        - Controllers are aware of other controllers, not super compatible with "Discard and reload" method of scene transitions
        - Custom JavaFX events which have both notification and payload capacity, would solve everything but lots of upfront work
        - Global singleton "state" containing "current" game states?
            - I don't love this as the singleton state is only currently useful for the game screen
        - PROBABLY BEST, JUST THOUGHT OF THIS - Ask for game details at game start screen?
            - So its technically the same "Scene", and I wouldn't have to transfer information between scenes
            - GameSceneController flag to denote whether to ask for settings or not
                - Entering from game screen --> Ask for settings!
                - Restarting same game --> don't ask
                - Restarting game with new settings --> ask!
                - May be a bit weird with multiplayer......? two copies of controllers asking
                    - but then again most things WILL break when implementing multiplayer (Didn't design ahead of time)

After some thought realising that moving the setting screen to the game controller doesn't matter, as we swap out the controller with a newly created
one in order to "reset" the game scene, so there are no persistent variables that can be used.
- Just go with Custom FX events with a dummy event node to "fire" said event, and then at the stage level attach a listener to perform the scene transitions
    - Replace current property-centric stage transition mechanisms to this
    - Benefits
        - Usable on any controller pretty easily, just dummy node, one time setup of custom events
        - The same stage is used throughout the lifecycle of the program, so we only attach ONE handler ONCE to the stage as FILTER, all controllers can use that handler
        - Stays de-coupled within the FX environment
        - "Makes sense", logic is easily followed if familar with FX events
todo:
1. Create custom events
2. Replace current scene transition properties
    - Add dummy nodes
    - Configure handlers to fire event with payload instead
3. Implement setting screen and menu -> game transition.

Created new event
Now just implementing it into gamescenecontroller
- Detach "settings screen" from menu scene specifically, create a new overlay scene at the stage level for it.
- This way the settings can be saved stage-level, and at the start of each game we can either use those settings or ask for new ones

ScratchFX:
- Attached new listener for scene transitions on Stage
- Removed property listener for loadGameScene(Still need to do for loadMenu)
- Created sceneTransitionHandler, only accounts for TO_GAME atm

Now I need the settings scene + controller

Scene transitions done, now finish the initial task of testing from both white + black perspectives

Back on task: 
1. Configure controller to respond to game-end conditions
2. Test all game end conditions
    - White/Black timeout - works
    - White/black checkmate - Works
    - stalemate - works
    - 75 move draw - works
    - 5 fold repetition draw - works
    - Insufficient material - works for KvK
3. Work on end game conditions not already accounted for:
    - Mutual draw
    - Resignation
    - Claiming 50 move / 3 fold repetition draw
4. Test everything to date from Black's perspective as well

Tested as much as I could with current framework for black + white, whats left to test is:
- All Insuf.Mat. cases for both sides
    - Figure out a way to do this programmatically, with a moveset as input?
    - May have to just implement replay functionality to do this, or at least begin work on it
- Mutual draw
    - Requires 2 real players
- Resignation
    - Implementation depends on use case
        - Saving PGN afterwards
        - 1 player vs BOT or pvp
- Optional draws at 3 fold repeat and 50 moves
    - Really only relevant for PvP and online play

Todo
- Fix git merge and branches, get better at branching
- Fix initial time displays, they are initially from white perspective always

commit - 
- Added few more test cases to StockfishTest, manual validation of output
    - Automated validation would require replay functionality as move validity depends on board state
        - "Replay" through a game with associated Board logic
        - For each subsequent board position, feed FEN into stockfish and get best move
        - Validate best move ensuring that it is legal for the Board state at that time for the given player
    - In conjunction with PGN parsing, can test thousands of full length games
        - This would make up a "Full test" testing all parts at once
            - Game logic, PGN parsing to moveset, Input PGN validity, StockfishClient (stockfish is assumed valid) interactions, replay functionality

now continue work on StockfishClient integration
Integrating into existing game loop logic:
    - Currently GUI moves are executed in onMouseRelease() and onMousePressed() in different circumstances
    - Just add another method getOpponentMove() after attemptMove() and block until recieved
        - getOpponentMove() can read the move from StockfishClient, and perform game logic in a similar way to the player
        - Then execution unblocks and the player can move again
        - Concurrency? 
            - Make opponent pieces mouse transparent in this mode

todo 
- create getOpponentMove() and insert it appropriately
    - Have it check for the existence of an opponent, if not the method does nothing
- Add options to settings screen (freeplay + vs bot)
    - Freeplay has no stockfish, all pieces are moveable
    - vs has stockfish, opponent pieces/Squares are mouse transparent

Where does stockfish reside?
- To start with just create a new instance at each game instance
- If too slow, add loading screen or keep persistent stockfish process at stage level
- CLEANUP on both levels to be safe 

GameSettings - May be a good idea to create a Settings object instead of using an int[]

- Remove ability to move around opponent pieces
- Modified code in both onMousePress and onMouseRelease to prevent dragging of opponent pieces if an opponent exists in the controller
- done? - need test

- Create wrapper method for everything required for a GUI Move (movePiece, highlightMove, updateClock, updateInfo, opponent move)
    - Good to have it all in one place
    - Currently there are 2 separate locations where moves are executed (onMousePress, onMouseRelease) in different circumstances
    - Also ENSURES consistent move behaviour regardless of method of execution
done

During testing realized PROMOTION was not TESTED
- Need a way to prompt user for promotion piece
- Need to extract promotion out of stockfish
ALSO CASTLING - Ensure proper formatting etc

Also I need stockfish to make the first move if its white

Before fixing promotion, test everything else first
- Prevent clicking opponent pieces
    - white DONE
    - black DONE
- Playing vs stockfish from both perspectives
    - white DONE
    - black DONE

Taking break - hitting gym
todo when back:
    - Figure out why sometimes we get invalid index during moves
        - May have to print debug lines for Board and scene graph (which squares have children);
    - Add promotion implementation
    - Ensure castling works properly

Created debug method that traverses all chess square stackpane nodes and reads their child contents, and displays as a string represention of the chess board
- Attempt to fix bug where .get(0) is invoked on the children of a childless stackpane square

bug: highlighted white move from black's perspective is incorrect, highlights blacks squares instead

Fixed faulty logic when playing white move from stockfish on black player perspective - remember allSquares is the 'true' position/board indices, which maps black's perspective
to the true position

Promotion options: 
- Promotion detection logic in controller/gui, passed on to attempt move
- Event/callback to Gui

lotsa problems
promotion
- player almost done although it seems square 7 acts weird and chrashes when promoting onto it
- haven't done stockfish promotion moves yet
- need to also add promotion checking move to mouse press handler - DONE

Fixed an issue where occassionally a promotion move would not register as a promotion move and skipped prompting for a promotion piece, resulting in a crash
- Due to confusion about the sentinel values returned by GUIEngine.findMove() when invoked within GUIEngine.isPromotionMove(), was checking for > 0 when should have been >= 0 (The move to the square in question was at index 0 of validMoves)

Revisit Stockfish lifecycle:
- Quite a noticable performance drop when comparing freeplay to 'vs' game mode - extra overhead from creating Stockfish process and waiting for uciok signal
- Not sure if its worth it to keep a process alive at stage level, there was already a loading lag transitioning to the game scene, and the increase is significantly impactful due to that

Overload PGNMoveValidator() to take an ArrayList<String> for already separated moves (from RegexGameData.moves)